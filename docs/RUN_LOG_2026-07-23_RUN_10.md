# 2026-07-23 - Run 10: Manual schedule overrides

This entry belongs chronologically after Run 9 in `docs/RUN_LOG.md`. It is stored separately because the available repository connector can replace files but cannot append safely to the existing long run log without reconstructing and risking deletion of earlier entries. A later shell-enabled maintenance run should append this entry verbatim to `docs/RUN_LOG.md` and then remove this supplemental file.

## Steps taken

- Read the current README, implementation plan, handoff, and complete available run history before making changes.
- Inspected the latest unit lookup, resolved schedule, Tehran date, and schedule model files.
- Added `src/config/scheduleOverrides.ts` as the operator-maintained override configuration, empty by default.
- Added `src/domain/scheduleOverrides.ts` with canonical Gregorian Saturday keys, per-day/per-slot replacements, date validation, position validation, unit-range validation, duplicate-position detection, and final private-unit invariant validation.
- Updated `src/domain/resolvedSchedule.ts` so every generated week is passed through configured overrides before being returned to live status, navigation, and unit lookup consumers.
- Added `src/domain/scheduleOverrides.test.ts` covering empty configuration, a two-unit swap, public and cleaning-slot replacement behavior, invalid day and slot indexes, duplicate units, unrelated weeks, malformed keys, and non-Saturday keys.
- Updated README completion status and handoff documentation.

## Verification

- Statically reviewed the new configuration, override domain module, tests, and resolved-schedule integration.
- Confirmed empty production configuration preserves generated schedules and both screenshot anchor sequences.
- Confirmed override application occurs inside `resolveWeeklySchedule`, before existing status and unit lookup calls receive the schedule.
- Confirmed private-unit changes are rejected unless the final week contains units 1 through 39 exactly once.
- Full dependency installation, Vitest execution, linting, TypeScript project checking, Vite production build, and browser rendering remain pending because the connector provides no repository shell or preview.

## Decisions

- Use Gregorian Saturday strings such as `2026-07-18` as stable override keys.
- Keep production configuration empty until management supplies a real exception.
- Apply overrides centrally in schedule resolution so all UI features share one final schedule.
- Permit explicit public or cleaning replacements while preserving the 39-private-unit invariant.
- Reject duplicate override positions rather than relying on array order.

## Issues

- The connector could not safely append to the existing long `docs/RUN_LOG.md`; this supplemental entry preserves the run without deleting history and must be merged later.
- Automated repository checks and browser verification have not yet run.

## Steps left

- Add GitHub Actions for install, lint, type checking, tests, and production build.
- Resolve any failures reported by CI and generate a lockfile through a verified install if feasible.
- Add static deployment configuration, browser verification, and final maintenance documentation.
- Merge this supplemental entry into `docs/RUN_LOG.md` when a safe append mechanism is available.

## Next recommended task

Add a GitHub Actions workflow that installs dependencies and runs `npm run lint`, `npm run typecheck`, `npm run test:run`, and `npm run build` on pushes and pull requests. Resolve any failures found by CI before adding deployment.
